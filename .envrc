# Point this at an orchestrator run folder that contains:
#   AssetManifest.json
#   RenderPlan.json (optional, depending on your script)
#   and where we will write AssetManifest.media.json
export RUN_DIR="/tmp/orch-artifacts/phase0-demo/run-4aab1f4530ea"

# Optional: where the local resolver looks for assets
# (defaults to ./data/local_assets in your implementation)
export LOCAL_ASSETS_ROOT="/home/tnnd/data/code/media/data/local_assets"
